import {NextRequest,NextResponse} from 'next/server';import {prisma} from '../../../../../../lib/prisma';const allowed=['IN_REVIEW','CHANGES_REQUESTED','APPROVED','PUBLISHED','REJECTED'];export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const {status}=await req.json();if(!allowed.includes(status))return NextResponse.json({error:'Invalid moderation status'},{status:400});try{return NextResponse.json(await prisma.release.update({where:{id},data:{status}}))}catch{return NextResponse.json({error:'Release not found'},{status:404})}}