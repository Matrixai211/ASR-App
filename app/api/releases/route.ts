import {NextRequest,NextResponse} from 'next/server';import {prisma} from '../../../lib/prisma';export async function GET(){const releases=await prisma.release.findMany({include:{tracks:true,artist:true},orderBy:{createdAt:'desc'}});return NextResponse.json(releases)}export async function POST(req:NextRequest){try{const body=await req.json();if(!body.artistId||!body.title||!body.type)return NextResponse.json({error:'artistId, title and type are required'},{status:400});const release=await prisma.release.create({data:{artistId:body.artistId,title:body.title,type:body.type,genre:body.genre||null,language:body.language||null,exclusive:Boolean(body.exclusive),downloadable:Boolean(body.downloadable),priceCents:body.priceCents||null}});return NextResponse.json(release,{status:201})}catch{return NextResponse.json({error:'Unable to create release'},{status:500})}}