import {NextRequest,NextResponse} from 'next/server';export async function POST(req:NextRequest){const {tier}=await req.json();if(!['PREMIUM','ARTIST'].includes(tier))return NextResponse.json({error:'Unsupported tier'},{status:400});return NextResponse.json({status:'configuration_required',tier,message:'Connect a payment provider and webhook before accepting live payments.'},{status:501})}